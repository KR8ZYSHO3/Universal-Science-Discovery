---
phase: 04-ci-trust-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/repo_smoke/test_crosscheck_confirmed_gates.py
  - docs/CROSSCHECK.md
  - CHANGELOG.md
autonomous: true
requirements:
  - TRUST-02

must_haves:
  truths:
    - "Every CONFIRMED-capable repro/**/*.py is invoked in a crosscheck-repro.yml step that also contains grep -q \"RESULT: CONFIRMED\""
    - "workflow.count of grep -q \"RESULT: CONFIRMED\" equals the discovered CONFIRMED-capable count (today 4: habitat FSS, cluster, epidemic, Ising); GCC is not discovered"
    - "giant_component_fraction.py does not share a workflow step with grep -q \"RESULT: CONFIRMED\""
    - "docs/CROSSCHECK.md Run-mode parity CI column is CONFIRMED-only: four seeds yes; GCC **no** with no (Phase 4 TRUST-02) TODO wording"
    - "D-12: no marketing, DNS, arXiv, catalog waves, JS runners, promote CLI, or Phase 5 recommendations"
    - "D-13: no fabricated RESULT: CONFIRMED; GSD artifacts remain process metadata, not scientific evidence"
  artifacts:
    - path: "tests/repo_smoke/test_crosscheck_confirmed_gates.py"
      provides: "TRUST-02 text inventory + negative GCC assertion"
      contains: "test_confirmed_capable_repro_scripts_are_grepped_in_crosscheck_repro_workflow"
      min_lines: 70
    - path: "docs/CROSSCHECK.md"
      provides: "CONFIRMED-only grep policy in Run-mode parity"
      contains: "must not"
    - path: "CHANGELOG.md"
      provides: "Unreleased inventory + CONFIRMED-only policy bullet"
      contains: "test_crosscheck_confirmed_gates.py"
  key_links:
    - from: "tests/repo_smoke/test_crosscheck_confirmed_gates.py"
      to: ".github/workflows/crosscheck-repro.yml"
      via: "UTF-8 text split on - name:; POSIX path must share a step with CONFIRMED grep"
      pattern: "grep -q \"RESULT: CONFIRMED\""
    - from: "repro/**/*.py CONFIRMED-capable markers"
      to: "four static tee+grep steps"
      via: "stdout-token heuristic, never YAML status"
      pattern: "'CONFIRMED' if"
---

<objective>
Close TRUST-02 with a fail-closed pytest inventory: every CONFIRMED-capable `repro/**/*.py` is grepped `RESULT: CONFIRMED` in `.github/workflows/crosscheck-repro.yml`, and INCONCLUSIVE-only GCC is not.

Purpose: A future CONFIRMED-stdout protocol cannot silently skip CI. Unified gates means mapping, not a fifth trophy.

Output: `tests/repo_smoke/test_crosscheck_confirmed_gates.py`, honesty rewrite of the `docs/CROSSCHECK.md` Run-mode parity CI column, CHANGELOG Unreleased bullet. Workflow YAML is **not** rewritten.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ci-trust-hardening/04-CONTEXT.md
@.planning/phases/04-ci-trust-hardening/04-RESEARCH.md
@.planning/phases/04-ci-trust-hardening/04-PATTERNS.md
@tests/repo_smoke/test_catalog_regression.py
@tests/repo_smoke/test_crosscheck_artifacts.py
@.github/workflows/crosscheck-repro.yml
@docs/CROSSCHECK.md
@CHANGELOG.md
@repro/p-b-percolation-oncology-gcc/giant_component_fraction.py

<interfaces>
TRUST-02 mechanism is pytest-only (user lock / RESEARCH Open Question 1). Do **not** add `strategy.matrix`, `scripts/check_crosscheck_gates.py`, or `repro/CONFIRMED_GATES.yml`. Do **not** `yaml.safe_load` the workflow (PyYAML 1.1 turns top-level `on:` into `True`).

Repo-root pytest header (copy; inventory does **not** import subprocess/sys/yaml):

```python
from __future__ import annotations

from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[2]
```

UTF-8 text read (copy I/O from `scripts/build_crosscheck.py`; never YAML load):

```python
path.read_text(encoding="utf-8")
str(path.relative_to(REPO_ROOT).as_posix())
```

CONFIRMED-capable markers — a `repro/**/*.py` file matches if UTF-8 text contains **any** of:

- `'CONFIRMED' if` or `"CONFIRMED" if`
- `result = "CONFIRMED"` or `result = 'CONFIRMED'`
- literal `RESULT: CONFIRMED`

Verified in current seeds:

```python
# simulate_percolation_fss.py
print(f"RESULT: {'CONFIRMED' if passed else 'INCONCLUSIVE (increase TRIALS_PER_P for higher precision)'}")

# cluster_size_exponent.py
print(f"RESULT: {'CONFIRMED' if passed else 'INCONCLUSIVE (adjust P or L for clearer scaling)'}")

# epidemic_percolation_fss.py
print(f"RESULT: {'CONFIRMED' if passed else 'INCONCLUSIVE (increase SEEDS_PER_N for higher precision)'}")

# ising_critical_slowing.py
if passed:
    result = "CONFIRMED"
print(f"RESULT: {result}")
```

GCC matches **none** (do not “fix” the script):

```python
print(
    "RESULT: INCONCLUSIVE (thin synthetic lattice; not a clinical "
    "biomarker; not an FSS precision pass)"
)
return 0
```

Keep these four static workflow steps verbatim (D-02). Do **not** edit `.github/workflows/crosscheck-repro.yml`:

```yaml
- name: Habitat percolation FSS (expect CONFIRMED)
  run: |
    python repro/p-b-habitat-percolation-ecology-fss/simulate_percolation_fss.py | tee /tmp/fss.out
    grep -q "RESULT: CONFIRMED" /tmp/fss.out

- name: Cluster size exponent (expect CONFIRMED)
  run: |
    python repro/p-b-habitat-percolation-ecology-cluster-exponent/cluster_size_exponent.py | tee /tmp/cluster.out
    grep -q "RESULT: CONFIRMED" /tmp/cluster.out

- name: Epidemic percolation FSS (expect CONFIRMED)
  run: |
    python repro/p-b-percolation-epidemiology-fss/epidemic_percolation_fss.py | tee /tmp/epidemic.out
    grep -q "RESULT: CONFIRMED" /tmp/epidemic.out

- name: Ising social dynamics EWI (expect CONFIRMED)
  run: |
    python repro/p-b-ising-social-dynamics-ewi/ising_critical_slowing.py | tee /tmp/ising.out
    grep -q "RESULT: CONFIRMED" /tmp/ising.out
```

Expected today: 4 discovered; GCC not discovered; `workflow.count('grep -q "RESULT: CONFIRMED"') == 4`.

Pickup: `.github/workflows/validate-schemas.yml` already runs `python -m pytest tests/repo_smoke -v --tb=short`. Do **not** duplicate pytest inside `crosscheck-repro.yml` (D-09).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Write CONFIRMED-gate inventory pytest (TRUST-02)</name>
  <files>tests/repo_smoke/test_crosscheck_confirmed_gates.py</files>
  <read_first>tests/repo_smoke/test_catalog_regression.py, tests/repo_smoke/test_crosscheck_artifacts.py, .github/workflows/crosscheck-repro.yml, repro/p-b-percolation-oncology-gcc/giant_component_fraction.py, .planning/phases/04-ci-trust-hardening/04-PATTERNS.md, .planning/phases/04-ci-trust-hardening/04-CONTEXT.md</read_first>
  <action>
Implement D-01, D-02, D-03, D-04, D-09, D-10, D-13. Discretion lock: pytest text inventory only — no Actions matrix, no `scripts/` helper, no `yaml.safe_load` of the workflow.

Create `tests/repo_smoke/test_crosscheck_confirmed_gates.py` with **exactly** this content (allowed: extra blank lines / a longer module docstring; do not add PyYAML, subprocess, or networkx):

```python
"""Inventory: CONFIRMED-capable repro scripts must be grepped in crosscheck-repro.yml.

Parse the workflow as UTF-8 text. Do not import yaml and do not yaml.safe_load
the workflow: PyYAML 1.1 turns the top-level on: key into True.
Source of truth is stdout RESULT markers in repro/**/*.py, not YAML status.
"""
from __future__ import annotations

from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[2]
REPRO_ROOT = REPO_ROOT / "repro"
WORKFLOW = REPO_ROOT / ".github" / "workflows" / "crosscheck-repro.yml"

CONFIRMED_GREP = 'grep -q "RESULT: CONFIRMED"'

_MARKERS = (
    "'CONFIRMED' if",
    '"CONFIRMED" if',
    'result = "CONFIRMED"',
    "result = 'CONFIRMED'",
    "RESULT: CONFIRMED",
)


def _repro_py_files() -> list[Path]:
    return [
        p
        for p in sorted(REPRO_ROOT.rglob("*.py"))
        if "__pycache__" not in p.parts
    ]


def _is_confirmed_capable(text: str) -> bool:
    return any(marker in text for marker in _MARKERS)


def _posix_rel(path: Path) -> str:
    return path.relative_to(REPO_ROOT).as_posix()


def _workflow_steps(workflow: str) -> list[str]:
    parts = workflow.split("- name:")
    return parts[1:] if len(parts) > 1 else parts


def test_confirmed_capable_repro_scripts_are_grepped_in_crosscheck_repro_workflow() -> None:
    workflow = WORKFLOW.read_text(encoding="utf-8")
    steps = _workflow_steps(workflow)
    discovered: list[str] = []
    for path in _repro_py_files():
        text = path.read_text(encoding="utf-8")
        if _is_confirmed_capable(text):
            discovered.append(_posix_rel(path))

    missing: list[str] = []
    for rel in discovered:
        paired = any(rel in step and CONFIRMED_GREP in step for step in steps)
        if not paired:
            missing.append(rel)

    grep_count = workflow.count(CONFIRMED_GREP)
    assert not missing, (
        f"CONFIRMED-capable scripts missing a CONFIRMED grep step: {missing}; "
        f"discovered={discovered}; grep_count={grep_count}"
    )
    assert grep_count == len(discovered), (
        f"grep count {grep_count} != discovered {len(discovered)}: {discovered}"
    )


def test_inconclusive_only_scripts_are_not_grepped_confirmed() -> None:
    workflow = WORKFLOW.read_text(encoding="utf-8")
    steps = _workflow_steps(workflow)
    for path in _repro_py_files():
        text = path.read_text(encoding="utf-8")
        if _is_confirmed_capable(text):
            continue
        rel = _posix_rel(path)
        for step in steps:
            assert not (rel in step and CONFIRMED_GREP in step), (
                f"{rel} must not share a step with {CONFIRMED_GREP}"
            )
```

Pairing rule (D-04): split workflow text on `- name:`. For each discovered script, the relative POSIX path (`repro/p-b-.../file.py`) must appear in a step whose body also contains `grep -q "RESULT: CONFIRMED"`. Assert `workflow.count('grep -q "RESULT: CONFIRMED"') == len(discovered)`. For every repro `*.py` that is **not** CONFIRMED-capable (today: `giant_component_fraction.py`), no step may contain both that script path and a CONFIRMED grep.

Do **not** read `protocols-catalog/**/*.yaml` `status:`. Habitat/cluster/Ising YAML stay `executed`; epidemic stays `confirmed`; GCC stays `ready`.

Do **not** edit:

- `.github/workflows/crosscheck-repro.yml` (keep four static greps; no matrix; no GCC step; no pytest job)
- `.github/workflows/validate-schemas.yml`
- `epidemic_percolation_fss.py` / freeze pcs / `NU_THEORY`
- habitat/cluster/Ising YAML status
- `giant_component_fraction.py`
- `scripts/generate_crosscheck.py`
- `dashboard/index.html`
- `docs/CROSSCHECK.md` (task 2)
- `CHANGELOG.md` (task 3)
- `tests/repo_smoke/test_crosscheck_repro_regression.py`

Do **not** create `scripts/check_crosscheck_gates.py` or `repro/CONFIRMED_GATES.yml`.
  </action>
  <acceptance_criteria>
    - File `tests/repo_smoke/test_crosscheck_confirmed_gates.py` exists
    - File defines `def test_confirmed_capable_repro_scripts_are_grepped_in_crosscheck_repro_workflow()`
    - File defines `def test_inconclusive_only_scripts_are_not_grepped_confirmed()`
    - File contains `workflow.split("- name:")`
    - File contains `'CONFIRMED' if` and `result = "CONFIRMED"` and `RESULT: CONFIRMED`
    - File contains `grep -q "RESULT: CONFIRMED"`
    - Non-comment non-docstring code does not contain `import yaml` or `yaml.safe_load` or `shell=True` or `strategy.matrix`
    - File `scripts/check_crosscheck_gates.py` does not exist
    - `.github/workflows/crosscheck-repro.yml` still contains exactly four `grep -q "RESULT: CONFIRMED"` and does not contain `giant_component_fraction.py`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/repo_smoke/test_crosscheck_confirmed_gates.py -x --tb=short</automated>
  </verify>
  <done>
Inventory pytest is green against current main: four CONFIRMED-capable seeds paired with four static greps; GCC is INCONCLUSIVE-only and is not grepped CONFIRMED. Workflow YAML is unchanged.
  </done>
</task>

<task type="auto">
  <name>Rewrite CROSSCHECK.md CI column to CONFIRMED-only policy</name>
  <files>docs/CROSSCHECK.md</files>
  <read_first>docs/CROSSCHECK.md, .planning/phases/04-ci-trust-hardening/04-PATTERNS.md, dashboard/index.html</read_first>
  <action>
Implement D-11. 04-01 owns `docs/CROSSCHECK.md`; 04-02 must not edit it (D-14).

In `docs/CROSSCHECK.md` `## Run-mode parity` table, keep the four seed `CI grep CONFIRMED` cells as `yes`. Replace **only** the GCC CI cell that currently reads `**no** (Phase 4 TRUST-02)` (line 88). That wording reads like Phase 4 might add a CONFIRMED grep.

Replace this exact row:

```markdown
| `p-b-percolation-oncology-gcc` | `giant_component_fraction.py` (`L=32`, `TRIALS=8`) | **no** | **no** | **no** (Phase 4 TRUST-02) | stdout `RESULT: INCONCLUSIVE`; Python `return 0` always |
```

with:

```markdown
| `p-b-percolation-oncology-gcc` | `giant_component_fraction.py` (`L=32`, `TRIALS=8`) | **no** | **no** | **no** | stdout `RESULT: INCONCLUSIVE`; Python `return 0` always |
```

Immediately after the table (before the `---` that precedes `## Contributing a protocol`), add this policy paragraph verbatim:

```markdown
CONFIRMED-only grep policy: four seed scripts are grepped `RESULT: CONFIRMED` in `.github/workflows/crosscheck-repro.yml`. GCC always prints `RESULT: INCONCLUSIVE` and **must not** be grepped CONFIRMED. Pytest covers the GCC entry point (`tests/repo_smoke/`); `validate-schemas.yml` runs that bundle on every PR.
```

Do **not** change the four seed rows. Do **not** change the internal Roadmap Phase 3/4 rows later in the file (those are Crosscheck product phases, not GSD). Do **not** edit `dashboard/index.html` (hub already links `#run-mode-parity`). Do **not** run `python scripts/build_crosscheck.py --apply`. After the docs edit, run `mkdocs build --strict`.
  </action>
  <acceptance_criteria>
    - `docs/CROSSCHECK.md` contains `CONFIRMED-only grep policy: four seed scripts are grepped \`RESULT: CONFIRMED\` in \`.github/workflows/crosscheck-repro.yml\`.`
    - `docs/CROSSCHECK.md` contains `GCC always prints \`RESULT: INCONCLUSIVE\` and **must not** be grepped CONFIRMED.`
    - `docs/CROSSCHECK.md` contains `Pytest covers the GCC entry point (\`tests/repo_smoke/\`); \`validate-schemas.yml\` runs that bundle on every PR.`
    - `docs/CROSSCHECK.md` does not contain `(Phase 4 TRUST-02)`
    - GCC row still contains `p-b-percolation-oncology-gcc` and `**no**` in the CI column without a TRUST-02 parenthetical
    - Four seed ids `p-b-habitat-percolation-ecology-fss`, `p-b-habitat-percolation-ecology-cluster-exponent`, `p-b-ising-social-dynamics-ewi`, `p-b-percolation-epidemiology-fss` remain in the table
    - `dashboard/index.html` is unmodified by this plan
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('docs/CROSSCHECK.md').read_text(encoding='utf-8'); assert 'CONFIRMED-only grep policy' in t; assert 'must not' in t; assert 'validate-schemas.yml' in t; assert '(Phase 4 TRUST-02)' not in t; assert 'p-b-percolation-oncology-gcc' in t; ids=['p-b-habitat-percolation-ecology-fss','p-b-habitat-percolation-ecology-cluster-exponent','p-b-ising-social-dynamics-ewi','p-b-percolation-epidemiology-fss']; assert all(i in t for i in ids); print('CROSSCHECK.md CONFIRMED-only policy OK')" && mkdocs build --strict</automated>
  </verify>
  <done>
Run-mode parity CI column states CONFIRMED-only policy. GCC stays **no**. MkDocs strict build passes. Hub was not regenerated.
  </done>
</task>

<task type="auto">
  <name>Add CHANGELOG Unreleased inventory bullet</name>
  <files>CHANGELOG.md</files>
  <read_first>CHANGELOG.md</read_first>
  <action>
Serialize `CHANGELOG.md` (04-01 then 04-02). Do not invent scientific claims.

Immediately after the `## [Unreleased]` heading in `CHANGELOG.md`, insert this section (newest first; leave the existing `### Added — Crosscheck generate/promote path and run-mode parity` block intact below it):

```markdown
### Added — Crosscheck CONFIRMED-gate inventory
- **`tests/repo_smoke/test_crosscheck_confirmed_gates.py`:** Text inventory of CONFIRMED-capable `repro/**/*.py` vs `.github/workflows/crosscheck-repro.yml` (four `grep -q "RESULT: CONFIRMED"` steps). GCC is INCONCLUSIVE-only and must not share a CONFIRMED grep step.
- **`docs/CROSSCHECK.md`:** Run-mode parity CI column states CONFIRMED-only grep policy (four seeds grepped CONFIRMED; GCC must not be).
```

Do not edit `README.md`, `docs/DOC_MAP.md`, or `docs/REPOSITORY_MANIFEST.md` (pytest pickup is already `tests/repo_smoke`). Do not edit `docs/CROSSCHECK.md` in this task (task 2 owns it).
  </action>
  <acceptance_criteria>
    - `CHANGELOG.md` under `## [Unreleased]` contains `### Added — Crosscheck CONFIRMED-gate inventory`
    - `CHANGELOG.md` contains `tests/repo_smoke/test_crosscheck_confirmed_gates.py`
    - `CHANGELOG.md` contains `CONFIRMED-only grep policy`
    - `CHANGELOG.md` still contains `### Added — Crosscheck generate/promote path and run-mode parity`
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('CHANGELOG.md').read_text(encoding='utf-8'); u=t.split('## [Unreleased]',1)[1]; assert '### Added — Crosscheck CONFIRMED-gate inventory' in u; assert 'tests/repo_smoke/test_crosscheck_confirmed_gates.py' in u; assert 'CONFIRMED-only grep policy' in u; assert '### Added — Crosscheck generate/promote path and run-mode parity' in u; print('CHANGELOG 04-01 OK')"</automated>
  </verify>
  <done>
Unreleased names the inventory pytest and the CONFIRMED-only docs policy. 04-02 can append its own Unreleased bullet next.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest inventory → committed workflow + repro sources | Test reads git files only; no PR title / GitHub context interpolation |
| `crosscheck-repro.yml` `run:` → runner shell | Untrusted context must never enter `run:` strings |
| stdout `RESULT:` token → scientific claim | CONFIRMED grep is a claim surface; GCC must not be grepped CONFIRMED |

ASVS L1 (ASVS 5.0 V2 validation / V1 injection): list-argv and static `run:` blocks. Block on HIGH.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `.github/workflows/crosscheck-repro.yml` `run:` | mitigate | Do not add `strategy.matrix` into `run:`. Keep the four static `run:` blocks. Inventory is pytest, not a matrix rewrite. |
| T-04-02 | Tampering | inventory parser | mitigate | Text parse only (`Path.read_text` + split on `- name:`). Do not `yaml.safe_load` the workflow (PyYAML `on:` → `True`). |
| T-04-03 | Tampering | pytest subprocess | mitigate | This plan has no subprocess. Do not introduce `shell=True`. |
| T-04-04 | Tampering | generate `--write` | mitigate | This plan does not call `generate_crosscheck.py`. Do not add `--write`. |
| T-04-05 | Integrity / Repudiation | GCC / CONFIRMED grep | mitigate | Negative inventory: `giant_component_fraction.py` must not share a step with `grep -q "RESULT: CONFIRMED"`. Never add a fifth CONFIRMED grep. Never fabricate `RESULT: CONFIRMED` (D-13). |

Block on HIGH. All five threats are mitigated by this plan. Do not invent extra security work (SHA-pinning Actions, CODEOWNERS, Scorecard).
</threat_model>

<verification>
- `python -m pytest tests/repo_smoke/test_crosscheck_confirmed_gates.py -x --tb=short` passes
- `.github/workflows/crosscheck-repro.yml` still has exactly four `grep -q "RESULT: CONFIRMED"` and no `giant_component_fraction.py`
- `docs/CROSSCHECK.md` has CONFIRMED-only policy and no `(Phase 4 TRUST-02)`
- `mkdocs build --strict` passes
- No `--watch` flags
</verification>

<success_criteria>
- TRUST-02 inventory is green: four CONFIRMED-capable scripts ↔ four workflow greps
- GCC is not grepped CONFIRMED
- Run-mode parity CI column states CONFIRMED-only policy
- Workflow YAML, epidemic freeze, generate script, GCC script, and hub HTML are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-trust-hardening/04-01-SUMMARY.md`
</output>
