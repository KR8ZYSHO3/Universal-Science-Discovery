---
phase: 02-epidemic-fss-precision
plan: 02
type: tdd
wave: 2
depends_on:
  - "02-01"
files_modified:
  - tests/repo_smoke/test_crosscheck_repro_regression.py
  - .github/workflows/crosscheck-repro.yml
autonomous: true
requirements:
  - CROSS-04
must_haves:
  truths:
    - "Fixed-input epidemic fit regression test passes in repo_smoke"
    - "CI crosscheck-repro workflow fails if epidemic repro loses CONFIRMED"
  artifacts:
    - path: tests/repo_smoke/test_crosscheck_repro_regression.py
      provides: test_epidemic_fss_fit_confirmed_on_reference_pcs
      contains: "test_epidemic_fss"
    - path: .github/workflows/crosscheck-repro.yml
      provides: Epidemic CONFIRMED grep gate
      contains: "grep -q \"RESULT: CONFIRMED\""
  key_links:
    - from: tests/repo_smoke/test_crosscheck_repro_regression.py
      to: repro/p-b-percolation-epidemiology-fss/epidemic_percolation_fss.py
      via: importlib fit_nu on reference pcs
      pattern: "epidemic_percolation_fss"
---

<objective>
Add regression test and CI CONFIRMED gate for epidemic FSS — mirror Phase 1 habitat/cluster/ising pattern.

Purpose: Prevents precision regressions; closes WORK-01 CI loop for 4th protocol.
Output: Passing pytest + grep gate in `crosscheck-repro.yml`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-epidemic-fss-precision/02-01-SUMMARY.md
@tests/repo_smoke/test_crosscheck_repro_regression.py
@.github/workflows/crosscheck-repro.yml
@repro/p-b-percolation-epidemiology-fss/epidemic_percolation_fss.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture reference p_c vector from CONFIRMED run</name>
  <files>tests/repo_smoke/test_crosscheck_repro_regression.py</files>
  <read_first>repro/p-b-percolation-epidemiology-fss/epidemic_percolation_fss.py, tests/repo_smoke/test_crosscheck_repro_regression.py, repro/p-b-habitat-percolation-ecology-fss/simulate_percolation_fss.py</read_first>
  <action>
Run `python repro/p-b-percolation-epidemiology-fss/epidemic_percolation_fss.py` after 02-01 and record the five per-N `p_c_hat` values from stdout.

Add `test_epidemic_fss_fit_confirmed_on_reference_pcs()` to `test_crosscheck_repro_regression.py`:
- Load module via `_load_module("epidemic_percolation_fss", REPO_ROOT / "repro/p-b-percolation-epidemiology-fss/epidemic_percolation_fss.py")`
- Set `pcs = [..., ..., ..., ..., ...]` with the exact 5 floats from CONFIRMED run (comment: date + settings)
- Call `mod.fit_nu(mod.SIZES, pcs)` — expect 3-tuple if 02-01 added sign_ok
- `rel_err = abs(nu - mod.NU_THEORY) / mod.NU_THEORY`
- Assert `sign_ok` is True
- Assert `rel_err <= mod.NU_TOLERANCE`
- Assert `r2 > 0.0`
  </action>
  <verify>python -m pytest tests/repo_smoke/test_crosscheck_repro_regression.py -k epidemic -v</verify>
  <acceptance_criteria>
    - test_crosscheck_repro_regression.py contains `def test_epidemic_fss_fit_confirmed_on_reference_pcs`
    - Test file comment includes `p-b-percolation-epidemiology-fss` and 5 reference floats
    - `pytest -k epidemic` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add CI CONFIRMED grep for epidemic</name>
  <files>.github/workflows/crosscheck-repro.yml</files>
  <read_first>.github/workflows/crosscheck-repro.yml</read_first>
  <action>
Change the epidemic step from bare run to tee + grep (match habitat/cluster/ising pattern):

```yaml
      - name: Epidemic percolation FSS (expect CONFIRMED)
        run: |
          python repro/p-b-percolation-epidemiology-fss/epidemic_percolation_fss.py | tee /tmp/epidemic.out
          grep -q "RESULT: CONFIRMED" /tmp/epidemic.out
```

Keep step name suffix `(expect CONFIRMED)` for consistency with other protocols.
  </action>
  <verify>python repro/p-b-percolation-epidemiology-fss/epidemic_percolation_fss.py 2>&1 | findstr /C:"RESULT: CONFIRMED"</verify>
  <acceptance_criteria>
    - crosscheck-repro.yml epidemic step contains `tee /tmp/epidemic.out`
    - crosscheck-repro.yml epidemic step contains `grep -q "RESULT: CONFIRMED" /tmp/epidemic.out`
    - crosscheck-repro.yml step name contains `expect CONFIRMED`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `python -m pytest tests/repo_smoke/test_crosscheck_repro_regression.py -k epidemic -v`
- [ ] `python -m pytest tests/repo_smoke -q` (full smoke still green)
</verification>

<success_criteria>
- Epidemic regression test locked to reference pcs
- CI grep gate matches other CONFIRMED protocols
- CROSS-04 satisfied in automated checks
</success_criteria>

<output>
After completion, create `.planning/phases/02-epidemic-fss-precision/02-02-SUMMARY.md`
</output>