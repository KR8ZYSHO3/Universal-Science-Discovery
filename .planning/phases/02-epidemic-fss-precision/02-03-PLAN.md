---
phase: 02-epidemic-fss-precision
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - protocols-catalog/physics-epidemiology/p-b-percolation-epidemiology-fss.yaml
  - repro/p-b-percolation-epidemiology-fss/README.md
  - repro/p-b-percolation-epidemiology-fss/run_crosscheck.ipynb
  - repro/p-b-percolation-epidemiology-fss/index.html
  - dashboard/explainers/b-percolation-epidemiology.html
  - dashboard/index.html
autonomous: true
requirements:
  - CROSS-04

must_haves:
  truths:
    - "Protocol YAML describes signed log-log volume FSS with nu_bar=3, theoretical p_c(inf)=1/MEAN_DEGREE, and S >= N**(-1/3), matching the script"
    - "Catalog no longer claims the fit target is nu=1"
    - "Colab notebook still invokes python epidemic_percolation_fss.py"
    - "Generated repro index.html is regenerated via build_crosscheck.py --apply, not hand-edited"
    - "Other seed protocols remain status: executed"
  artifacts:
    - path: "protocols-catalog/physics-epidemiology/p-b-percolation-epidemiology-fss.yaml"
      provides: "Honest experimental_design / stats / runtime / exponent / optional status confirmed"
      contains: "volume"
    - path: "repro/p-b-percolation-epidemiology-fss/README.md"
      provides: "Run command plus exit-code honesty"
      contains: "Exit code 0 always"
    - path: "repro/p-b-percolation-epidemiology-fss/run_crosscheck.ipynb"
      provides: "Colab demo calling the canonical script"
      contains: "python epidemic_percolation_fss.py"
    - path: "repro/p-b-percolation-epidemiology-fss/index.html"
      provides: "Generated Pages landing from YAML"
  key_links:
    - from: "p-b-percolation-epidemiology-fss.yaml experimental_design"
      to: "epidemic_percolation_fss.py estimator and fit"
      via: "same N grid, S threshold, PC_INF, log-linear nu_bar=3, SEEDS_PER_N"
      pattern: "N\\^\\(-1/3\\)"
    - from: "run_crosscheck.ipynb"
      to: "epidemic_percolation_fss.py"
      via: "!python epidemic_percolation_fss.py"
      pattern: "python epidemic_percolation_fss.py"
    - from: "YAML"
      to: "index.html"
      via: "python scripts/build_crosscheck.py --apply"
      pattern: "build_crosscheck.py"
---

<objective>
Align the epidemic protocol catalog, README, Colab notebook, and generated hub landing page with the plan-01 script (volume nu_bar=3, critical estimator, signed OLS) and verify the Colab path still runs that same `.py`.

Purpose: YAML today still says NLS + 10 seeds + giant fraction 50% + nu=1. Leaving it would fail CROSS-05 drift honesty even if stdout is CONFIRMED. ROADMAP 02-03 is verify-or-document Colab, not a JS runner.

Output: Updated epidemic YAML (status may become confirmed), README exit-code sentence, notebook markdown that no longer targets nu≈1, regenerated artifacts via `--apply`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-epidemic-fss-precision/02-01-SUMMARY.md
@protocols-catalog/physics-epidemiology/p-b-percolation-epidemiology-fss.yaml
@repro/p-b-percolation-epidemiology-fss/README.md
@repro/p-b-percolation-epidemiology-fss/run_crosscheck.ipynb
@schemas/protocol.yaml
@repro/p-b-ising-social-dynamics-ewi/README.md

<interfaces>
Schema `status` enum: `[draft, ready, executed, confirmed, falsified]`. Phase 1 left other seeds at `executed`. D-08: epidemic may flip to `confirmed` in this PR if Python CONFIRMED; do not batch-fix habitat/cluster/Ising YAML.

`build_crosscheck.py --apply` regenerates `repro/*/index.html`, hub grid in `dashboard/index.html`, and explainers. Do not hand-edit those HTML files.

Colab notebook cells today:
- markdown: claims "mean-field ν ≈ 1" (must change — D-03)
- code: clone + `%cd .../p-b-percolation-epidemiology-fss`
- code: `!pip install -q -r requirements.txt` then `!python epidemic_percolation_fss.py` (keep)

Do not add epidemic to `scripts/crosscheck_browser.py` BROWSER_RUNNERS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Rewrite epidemic protocol YAML to match the signed volume-FSS script</name>
  <files>protocols-catalog/physics-epidemiology/p-b-percolation-epidemiology-fss.yaml</files>
  <read_first>protocols-catalog/physics-epidemiology/p-b-percolation-epidemiology-fss.yaml, repro/p-b-percolation-epidemiology-fss/epidemic_percolation_fss.py, .planning/phases/02-epidemic-fss-precision/02-01-SUMMARY.md, schemas/protocol.yaml</read_first>
  <action>
Implement D-03 (YAML rewrite; never relabel 3 as 1) and D-08 (experimental_design / statistical_analysis_plan / estimated_runtime / exponent must match the script; epidemic status may become confirmed; do not batch-fix other seeds).

Read locked `SEEDS_PER_N` and `NU_TOLERANCE` from 02-01-SUMMARY.md CONFIRMED_FREEZE. Substitute those integers/floats into the YAML below. If freeze is missing, stop.

Overwrite `protocols-catalog/physics-epidemiology/p-b-percolation-epidemiology-fss.yaml` with this document (keep id, source_*, translation_mapping, references DOIs exactly; do not invent DOIs). Set `status: confirmed` because plan 01 already printed RESULT: CONFIRMED. Do not edit any other file under `protocols-catalog/`.

```yaml
id: p-b-percolation-epidemiology-fss
title: >
  Finite-size epidemic threshold — does bond percolation p_c(N) on ER graphs
  shift as |p_c(N)-p_c(inf)| ~ N^(-1/nu_bar) with volume exponent nu_bar = 3?
status: confirmed
source_bridge: b-percolation-epidemiology
source_hypothesis: h-percolation-outbreak-threshold
source_unknown: u-percolation-epidemic-fss
pollination_index: 0
falsifiable_prediction: >
  On Erdős–Rényi graphs G(N, p) with mean degree 6, the bond percolation
  threshold estimated by bisection until the mean giant fraction S is at least
  N^(-1/3) approaches p_c(inf)=1/6 from above as |p_c(N)-p_c(inf)| ~ N^(-1/nu_bar)
  with volume FSS exponent nu_bar = 3 (mean-field d_u=6 times lattice nu=1/2).
  The Bethe/chemical-distance exponent nu = 1 is a different quantity and is not
  the fit target. Accept if the signed log-log fit is sign-consistent and the
  relative error versus 3 is within documented NU_TOLERANCE.
null_hypothesis: >
  Percolation threshold estimates are size-independent, signs mix about
  p_c(inf)=1/6, or the fitted volume exponent differs from nu_bar = 3 by more
  than the documented tolerance.
translation_mapping:
  - field_a_term: bond percolation threshold p_c
    field_b_term: epidemic threshold R_0 = 1 (herd immunity boundary)
  - field_a_term: finite-size scaling (FSS) correction to p_c in N-node network
    field_b_term: correction to R_0 threshold in finite population of size N
  - field_a_term: universality class (mean-field percolation for random graphs)
    field_b_term: contact network topology class (random mixing population)
experimental_design:
  - Build Erdős–Rényi graphs G(N, p) for N in {200, 500, 1000, 2000, 5000} with mean degree 6 using NetworkX fast_gnp_random_graph.
  - For each N, generate SEEDS_PER_N graph instances (locked value from the repro script; default starts at 20). At each bisection mid-point, average the giant-component fraction S across those graphs, then bisect p until mean S is at least N^(-1/3).
  - Fit |p_c(N) - p_c(inf)| ~ N^(-1/nu_bar) with fixed theoretical p_c(inf) = 1/6 (Poisson ER / Newman T_c), using signed log-linear OLS (nu_bar = -1/slope). Require all p_c(N) above p_c(inf).
  - Compare fitted nu_bar to 3; accept if sign_ok and relative error is within NU_TOLERANCE (0.25 unless the locked repro tightened to 0.15).
  - Record graph seeds N*100+s, print per-N p_c_hat and delta versus 1/6, and print RESULT CONFIRMED or INCONCLUSIVE on stdout. Exit code is always 0.
statistical_analysis_plan: >
  Log-linear fit of (p_c(N) - 1/6) versus N with fixed p_c(inf) = 1/6;
  report slope, R^2, sign check, and nu_bar = -1/slope. This is ordinary
  least squares in log-log space, not nonlinear least squares and not a
  10-seed bootstrap. Chemical-distance nu = 1 is documented as a different
  exponent and is not the acceptance target.
feasibility_tier: desktop
repro_bundle: repro/p-b-percolation-epidemiology-fss/
estimated_runtime: under 1 minute on a modern laptop (SEEDS_PER_N=20 by default)
last_reviewed: "2026-08-26"
references:
  - doi: "10.1103/PhysRevE.66.016128"
    note: Newman (2002) — epidemic threshold = bond percolation threshold
  - arxiv: "cond-mat/9212004"
    note: Finite-size scaling toolkit
```

After writing, substitute the freeze `SEEDS_PER_N` and `NU_TOLERANCE` into the experimental_design bullets as concrete numbers (write `20` or `40`, write `25%` or `15%`). If freeze locked 40 seeds, change `estimated_runtime` to `under 1 minute on a modern laptop (SEEDS_PER_N=40 by default)` (or the measured wall_clock_s if it exceeded 60s). Do not restore the old `3–8 minutes` unless the timed 01 run actually took that long.

Do not change habitat, cluster, or Ising YAML `status` (D-08).
  </action>
  <acceptance_criteria>
- Epidemic YAML `status:` is `confirmed`
- YAML contains `nu_bar = 3` (or `nu_bar=3`) and `N^(-1/3)` (or `N^{-1/3}`)
- YAML contains `p_c(inf) = 1/6` or `1/MEAN_DEGREE` or `1/6`
- YAML does not contain `Nonlinear least squares`
- YAML does not contain `bootstrap 95% CI on nu across 10 seeds`
- YAML does not contain `crosses 50%`
- YAML `title` does not end with `at nu = 1?`
- No other `protocols-catalog/**/*.yaml` in this plan's diff besides this one file
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; import subprocess,sys; p=Path('protocols-catalog/physics-epidemiology/p-b-percolation-epidemiology-fss.yaml'); t=p.read_text(encoding='utf-8'); assert 'status: confirmed' in t or 'status: confirmed' in t.replace('\"',''); assert 'nu_bar' in t and '3' in t; assert '50%' not in t; assert 'Nonlinear least squares' not in t; assert '10 seeds' not in t; assert 'at nu = 1?' not in t; assert ('1/6' in t or '1/MEAN_DEGREE' in t); assert ('N^(-1/3)' in t or 'N^{-1/3}' in t or 'N**(-1/3)' in t); print('yaml strings OK')"; subprocess.check_call([sys.executable,'scripts/validate_schemas.py'])"</automated>
  </verify>
  <done>
Epidemic protocol YAML matches the script's estimator, PC_INF=1/6, signed log-log, volume exponent 3, locked seeds/tolerance, and may show status confirmed. Other seeds untouched.
  </done>
</task>

<task type="auto">
  <name>Document Colab path and honest README; fix notebook exponent claim</name>
  <files>repro/p-b-percolation-epidemiology-fss/README.md, repro/p-b-percolation-epidemiology-fss/run_crosscheck.ipynb</files>
  <read_first>repro/p-b-percolation-epidemiology-fss/README.md, repro/p-b-percolation-epidemiology-fss/run_crosscheck.ipynb, repro/p-b-ising-social-dynamics-ewi/README.md</read_first>
  <action>
ROADMAP 02-03: verify or document the Colab/notebook path. Do not add a JS browser runner (networkx is not stdlib; epidemic is absent from BROWSER_RUNNERS).

Overwrite `repro/p-b-percolation-epidemiology-fss/README.md` with these exact lines (the four-space block is a shell snippet inside the README):

# p-b-percolation-epidemiology-fss
<blank>
Crosschecks [`b-percolation-epidemiology`](../../cross-domain/physics-epidemiology/b-percolation-epidemiology.yaml) via bond percolation finite-size scaling on Erdős–Rényi graphs.
<blank>
This Crosscheck tests the **volume** FSS exponent nu_bar = 3 (mean-field d_u=6, lattice nu=1/2). The Bethe/chemical-distance exponent nu = 1 is a different quantity and is not the fit target.
<blank>
    pip install -r requirements.txt
    python epidemic_percolation_fss.py
<blank>
Exit code 0 always; inspect stdout for CONFIRMED vs INCONCLUSIVE.
<blank>
Demo tier is Google Colab (`run_crosscheck.ipynb`), which clones this repo and runs the same `epidemic_percolation_fss.py`. There is no in-browser JS runner.

Wrap the two indented command lines in a Markdown fenced bash block (triple backticks + bash) in the README, matching the Ising README shape. Keep the existing bridge relative link path.

Keep the existing bridge relative link path. Copy Ising's exit-code honesty; do not copy habitat README's "exit 1 = falsified" (the Python always returns 0). Generated `index.html` may still mention exit 1 because `scripts/generate_repro_index_pages.py` is shared — do not edit that generator in this phase.

Update only the markdown cell of `run_crosscheck.ipynb`. Replace `Bond percolation finite-size scaling on Erdős–Rényi graphs (mean-field ν ≈ 1).` with:

Bond percolation finite-size scaling on Erdős–Rényi graphs. This Crosscheck tests the volume FSS exponent nu_bar = 3 (mean-field d_u=6, lattice nu=1/2). Chemical-distance nu = 1 is a different quantity and is not the fit target.

Keep cell 0 as markdown with protocol id and `Canonical repro: epidemic_percolation_fss.py`. Keep the two code cells byte-for-byte:

```
!git clone --depth 1 https://github.com/KR8ZYSHO3/Universal-Science-Discovery.git
%cd Universal-Science-Discovery/repro/p-b-percolation-epidemiology-fss
```

```
!pip install -q -r requirements.txt
!python epidemic_percolation_fss.py
```

Do not add CLI flags to the script (notebook would then have to pass them). Do not change kernelspec. Optional hosted Colab click is manual; the automated substitute is this cell grep.
  </action>
  <acceptance_criteria>
- README contains `Exit code 0 always`
- README contains `nu_bar = 3` or `nu_bar=3`
- README contains `python epidemic_percolation_fss.py`
- Notebook JSON still contains `!python epidemic_percolation_fss.py`
- Notebook JSON still contains `!pip install -q -r requirements.txt`
- Notebook JSON does not contain `ν ≈ 1` or `nu ≈ 1` as the claimed target
- Notebook JSON contains `nu_bar` or `volume` in the markdown cell
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; r=Path('repro/p-b-percolation-epidemiology-fss/README.md').read_text(encoding='utf-8'); n=Path('repro/p-b-percolation-epidemiology-fss/run_crosscheck.ipynb').read_text(encoding='utf-8'); assert 'Exit code 0 always' in r; assert 'python epidemic_percolation_fss.py' in r; assert 'nu_bar' in r; assert '!python epidemic_percolation_fss.py' in n; assert '!pip install -q -r requirements.txt' in n; assert 'mean-field \u03bd \u2248 1' not in n; assert 'volume' in n or 'nu_bar' in n; print('readme+notebook OK')"</automated>
  </verify>
  <done>
README is honest about exit codes and nu_bar=3. Colab still runs the canonical Python file. No JS runner added.
  </done>
</task>

<task type="auto">
  <name>Regenerate Crosscheck artifacts from YAML</name>
  <files>repro/p-b-percolation-epidemiology-fss/index.html, dashboard/explainers/b-percolation-epidemiology.html, dashboard/index.html</files>
  <read_first>protocols-catalog/physics-epidemiology/p-b-percolation-epidemiology-fss.yaml, scripts/build_crosscheck.py</read_first>
  <action>
Per D-08: after YAML experimental_design / stats / runtime / exponent / status edits, run apply then check. Do not hand-edit generated HTML.

From repo root:

```
python scripts/build_crosscheck.py --apply
python scripts/build_crosscheck.py --check
```

`--apply` will rewrite `repro/p-b-percolation-epidemiology-fss/index.html` from the YAML title/prediction/runtime, refresh `dashboard/explainers/b-percolation-epidemiology.html`, and refresh the hub grid in `dashboard/index.html`. Commit those generated diffs if they change. If other `repro/*/index.html` files churn with no content change, do not keep spurious diffs.

Do not edit `scripts/generate_repro_index_pages.py` to fix the shared "exit code 1" sentence in this phase. Do not run `mkdocs`. Do not regenerate domain pages unless those files actually change (they should not).
  </action>
  <acceptance_criteria>
- `python scripts/build_crosscheck.py --check` exits 0
- `repro/p-b-percolation-epidemiology-fss/index.html` does not contain `at nu = 1?`
- `index.html` contains `nu_bar` or `volume exponent`
- `index.html` still links the Colab notebook `run_crosscheck.ipynb`
- Epidemic YAML is the only protocol YAML modified
  </acceptance_criteria>
  <verify>
    <automated>python scripts/build_crosscheck.py --check</automated>
  </verify>
  <done>
Generated landing/hub/explainer match the rewritten epidemic YAML. CROSS-05 drift gate stays green. Catalog no longer advertises nu=1 as the FSS target.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Protocol YAML → generated hub/index/explainer | Humans trust catalog text as the experiment |
| Notebook markdown → Colab users | Demo must not advertise a different exponent than the script |
| status: confirmed badge | Independent of stdout grep; must not batch-upgrade other seeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-11 | Tampering (integrity) | protocol YAML vs script | mitigate | Rewrite experimental_design/stats/runtime/exponent to match plan-01 script (D-03, D-08). No NLS, no 50% giant, no nu=1 target. |
| T-2-12 | Spoofing | YAML status / hub badge | mitigate | Epidemic may become confirmed only because Python CONFIRMED. Do not flip other seeds (D-08). |
| T-2-13 | Tampering | generated index.html | mitigate | Only `build_crosscheck.py --apply`; then `--check`. No hand edits. |
| T-2-14 | Information disclosure | catalog | accept | Synthetic protocol; Newman 2002 DOI already present — do not invent citations. No outbreak microdata. |
| T-2-15 | ASVS V5/V13 | n/a | accept | No user input, no API. |
</threat_model>

<verification>
- YAML python -c checks
- README + notebook python -c checks
- `python scripts/build_crosscheck.py --check`
- Optional later: `python -m pytest tests/repo_smoke/test_crosscheck_artifacts.py -q` (uses `--check`)
</verification>

<success_criteria>
- Epidemic YAML matches the CONFIRMED script (nu_bar=3, S threshold N**(-1/3), PC_INF=1/6, locked seeds)
- Colab notebook still runs `python epidemic_percolation_fss.py`
- `--check` is green
- Other protocol YAML files unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-epidemic-fss-precision/02-03-SUMMARY.md`. Note the locked SEEDS_PER_N/NU_TOLERANCE copied from 02-01, whether status is confirmed, and that Colab cells still call the canonical script.
</output>
