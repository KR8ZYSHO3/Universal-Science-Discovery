---
phase: 03-crosscheck-scale-up
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - docs/CROSSCHECK.md
  - scripts/generate_repro_index_pages.py
  - repro/p-b-habitat-percolation-ecology-fss/README.md
  - dashboard/index.html
  - repro/p-b-percolation-oncology-gcc/index.html
  - dashboard/explainers/b-percolation-oncology.html
  - repro/p-b-habitat-percolation-ecology-fss/index.html
  - repro/p-b-habitat-percolation-ecology-cluster-exponent/index.html
  - repro/p-b-ising-social-dynamics-ewi/index.html
  - repro/p-b-percolation-epidemiology-fss/index.html
  - CHANGELOG.md
  - README.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CROSS-06
  - CROSS-07

must_haves:
  truths:
    - "docs/CROSSCHECK.md documents python scripts/generate_crosscheck.py --bridge b-percolation-oncology --write and the drafts/crosscheck/physics-oncology/ output path"
    - "docs/CROSSCHECK.md has a Run-mode parity section whose table includes all four seed protocol ids plus p-b-percolation-oncology-gcc"
    - "Parity columns are protocol id, Python canonical script, browser JS, Colab notebook, CI grep CONFIRMED, RESULT contract"
    - "Python is canonical; browser and Colab are demo tier — stated explicitly"
    - "Generated repro index.html and habitat FSS README say exit 0 always; inspect stdout — not exit 1 = falsified"
    - "build_crosscheck.py --apply then --check pass; hub cards include the new protocol"
  artifacts:
    - path: "docs/CROSSCHECK.md"
      provides: "Generate/promote happy path + CROSS-07 parity matrix"
      contains: "b-percolation-oncology --write"
    - path: "scripts/generate_repro_index_pages.py"
      provides: "Exit-code honesty in generated landings"
      contains: "Exit code is always"
    - path: "repro/p-b-habitat-percolation-ecology-fss/README.md"
      provides: "Exit 0 always sentence"
      contains: "Exit code 0 always"
    - path: "dashboard/index.html"
      provides: "Parity link in static Crosscheck blurb + generated fifth card"
      contains: "run-mode-parity"
    - path: "repro/p-b-percolation-oncology-gcc/index.html"
      provides: "Generated Pages landing from --apply"
      contains: "p-b-percolation-oncology-gcc"
  key_links:
    - from: "dashboard/index.html #crosscheck section-desc"
      to: "docs/CROSSCHECK.md#run-mode-parity"
      via: "static manifesto + parity anchors (outside hub-grid markers)"
      pattern: "run-mode-parity"
    - from: "generate_repro_index_pages.py PAGE template"
      to: "repro/*/index.html"
      via: "python scripts/build_crosscheck.py --apply"
      pattern: "Exit code is always"
    - from: "p-b-percolation-oncology-gcc.yaml repro_bundle"
      to: "hub card + explainer #crosscheck"
      via: "render_crosscheck_hub.py / generate_explainers.py"
      pattern: "p-b-percolation-oncology-gcc"
---

<objective>
Close CROSS-07 (and document CROSS-06): author the four-seed-plus-GCC run-mode parity matrix in `docs/CROSSCHECK.md`, fix exit-code honesty in the index generator and habitat FSS README, surface the matrix from the hub, then `--apply` / `--check`.

Purpose: Researchers must see that Python is canonical and browser/Colab are lighter demo tiers, and that the new oncology protocol is local-only INCONCLUSIVE — not a fifth CONFIRMED trophy.

Output: manifesto parity section, honest landings, regenerated hub card + oncology explainer.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-crosscheck-scale-up/03-CONTEXT.md
@.planning/phases/03-crosscheck-scale-up/03-RESEARCH.md
@.planning/phases/03-crosscheck-scale-up/03-01-SUMMARY.md
@docs/CROSSCHECK.md
@scripts/generate_repro_index_pages.py
@scripts/build_crosscheck.py
@scripts/crosscheck_browser.py
@repro/p-b-habitat-percolation-ecology-fss/README.md
@dashboard/index.html
@protocols-catalog/physics-oncology/p-b-percolation-oncology-gcc.yaml

<interfaces>
D-05 parity columns (minimum, exact headers to use):
`protocol id | Python canonical | browser JS | Colab | CI grep CONFIRMED | RESULT contract`

Four seed ids that **must** appear (D-05) plus the 03-01 protocol:
- `p-b-habitat-percolation-ecology-fss`
- `p-b-habitat-percolation-ecology-cluster-exponent`
- `p-b-ising-social-dynamics-ewi`
- `p-b-percolation-epidemiology-fss`
- `p-b-percolation-oncology-gcc`

`BROWSER_RUNNERS` (do not add oncology):
```python
BROWSER_RUNNERS = {
    "p-b-habitat-percolation-ecology-fss": "simulate_percolation_fss.js",
    "p-b-habitat-percolation-ecology-cluster-exponent": "cluster_size_exponent.js",
    "p-b-ising-social-dynamics-ewi": "ising_critical_slowing.js",
}
COLAB_NOTEBOOK = "run_crosscheck.ipynb"
```

`--apply` order (`scripts/build_crosscheck.py`):
`generate_repro_index_pages.py` → `render_crosscheck_hub.py --apply` → `generate_explainers.py <bridge-ids>`.
Do not hand-edit `repro/*/index.html` or `dashboard/explainers/*.html`.
Hub grid lives between `<!-- @hub-crosscheck-grid-begin -->` and `<!-- @hub-crosscheck-grid-end -->`. Static `section-desc` is **outside** that snapshot — safe to add a parity `<a>`.
Generator path formula: `drafts/crosscheck/<cross-domain parent>/<protocol_id>.yaml`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Document generate/promote happy path and D-05 parity matrix in CROSSCHECK.md</name>
  <files>docs/CROSSCHECK.md</files>
  <read_first>docs/CROSSCHECK.md, .planning/phases/03-crosscheck-scale-up/03-CONTEXT.md, scripts/crosscheck_browser.py, .github/workflows/crosscheck-repro.yml, protocols-catalog/physics-oncology/p-b-percolation-oncology-gcc.yaml</read_first>
  <action>
Implement D-01 (docs half), D-05, D-11, D-17. Do **not** create `docs/CROSSCHECK_PARITY.md` (D-11: section in `docs/CROSSCHECK.md`).

03-01 must already have created `protocols-catalog/physics-oncology/p-b-percolation-oncology-gcc.yaml`. If missing, stop — do not invent the protocol here.

**Quick start (D-01).** Replace the current generate example that uses only `b-habitat-percolation-ecology` as the write example. Keep a dry-run of any single bridge if useful, but the **documented happy-path write** must be exactly:

```bash
python scripts/generate_crosscheck.py --bridge b-percolation-oncology --write
```

Immediately under that command, state the output path formula and the concrete destination:

- Formula: `drafts/crosscheck/<parent of the bridge YAML relative to cross-domain/>/<protocol_id>.yaml`
- Concrete: `drafts/crosscheck/physics-oncology/p-b-percolation-oncology-*.yaml` (opportunity 1 raw id `p-b-percolation-oncology-percolation-derived-metrics-giant-compon`)

Sentence required: do **not** use `--all` as the documented happy path (too noisy). Do not show `python scripts/generate_crosscheck.py --all --write` as a command to run.

**Contributing (D-02/D-17).** Replace “promote a draft … after filling in `experimental_design` and `falsifiable_prediction`” with the real gate:

- Fill `null_hypothesis`, `statistical_analysis_plan`, and honest `experimental_design` (not generator TODOs / not `[DRAFT]` title).
- **Manual copy + PR** into `protocols-catalog/<same parent as the bridge>/`. There is **no** Crosscheck promote CLI (D-17). Do not tell contributors to run `promote_wave_factory_batch.py` on protocols.
- Set `status: ready` (never `confirmed` unless a local run printed `RESULT: CONFIRMED`).
- Set `repro_bundle: repro/<protocol-id>/`.
- Then `python scripts/validate_schemas.py`.

**Catalog table.** The current table lists three seeds and omits Ising. Update it so it lists all four seeds **and** the new protocol:

| Protocol | Bridge | Tier |
| `p-b-habitat-percolation-ecology-fss` | Habitat fragmentation ↔ percolation | desktop (browser demo) |
| `p-b-habitat-percolation-ecology-cluster-exponent` | Same bridge, cluster size distribution | desktop (browser demo) |
| `p-b-ising-social-dynamics-ewi` | Ising ↔ social dynamics EWI | desktop (browser demo) |
| `p-b-percolation-epidemiology-fss` | Epidemic threshold ↔ bond percolation | desktop (Colab demo) |
| `p-b-percolation-oncology-gcc` | Tumor vasculature ↔ percolation GCC | desktop (local Python; INCONCLUSIVE) |

**D-05 / D-11 — new section `## Run-mode parity`** (anchor will be `#run-mode-parity`). Place it after Protocol catalog (before or after Contributing is fine; keep one `## Run-mode parity` heading). First sentence of the section, exact meaning:

`Python is canonical; browser and Colab are demo tier.`

Insert this table (columns are D-05; do not drop a column; all four seed ids plus GCC must appear as fenced code ids or backtick ids):

| protocol id | Python canonical | browser JS | Colab | CI grep CONFIRMED | RESULT contract |
|---|---|---|---|---|---|
| `p-b-habitat-percolation-ecology-fss` | `simulate_percolation_fss.py` (L∈{16,32,64,128}, `TRIALS_PER_P=350`) | yes, `simulate_percolation_fss.js` (same L, `TRIALS_PER_P=120`) | no | yes | stdout `RESULT:` token; Python `return 0` always |
| `p-b-habitat-percolation-ecology-cluster-exponent` | `cluster_size_exponent.py` (`P=0.59`, `L=256`, `SEEDS=20`) | yes, `cluster_size_exponent.js` (`P=0.592`, `L=128`) | no | yes | stdout `RESULT:` on success; can `return 1` if too few clusters / NaN fit |
| `p-b-ising-social-dynamics-ewi` | `ising_critical_slowing.py` (`LATTICE_SIZE=48`) | yes, `ising_critical_slowing.js` (`L=32`, lighter sweeps) | no | yes | stdout `RESULT:` token; Python `return 0` always |
| `p-b-percolation-epidemiology-fss` | `epidemic_percolation_fss.py` (networkx; freeze `NU_THEORY=3.0`) | **no** (not in `BROWSER_RUNNERS`; D-09) | yes, `run_crosscheck.ipynb` | yes | stdout `RESULT:` token; Python `return 0` always |
| `p-b-percolation-oncology-gcc` | `giant_component_fraction.py` (`L=32`, `TRIALS=8`) | **no** | **no** | **no** (Phase 4 TRUST-02) | stdout `RESULT: INCONCLUSIVE`; Python `return 0` always |

**Manifesto roadmap collision.** In the existing `## Roadmap` table that lists manifesto “Phase 3 / Phase 4” (results YAML / unified toolkit), add one prose sentence: those rows are **not** GSD Phase 3. GSD Phase 3 is generate/promote + this parity matrix. Do not implement results-YAML feedback or a unified percolation toolkit (deferred).

Do not add a JS runner for epidemic or oncology (D-09). Do not edit `scripts/crosscheck_browser.py`. Do not add a 5th CI grep (D-03). Do not change epidemic freeze constants (D-07).
  </action>
  <acceptance_criteria>
    - `docs/CROSSCHECK.md` contains `python scripts/generate_crosscheck.py --bridge b-percolation-oncology --write`
    - `docs/CROSSCHECK.md` contains `drafts/crosscheck/physics-oncology/`
    - `docs/CROSSCHECK.md` contains heading text `Run-mode parity` and the sentence `Python is canonical; browser and Colab are demo tier`
    - `docs/CROSSCHECK.md` contains all five ids: `p-b-habitat-percolation-ecology-fss`, `p-b-habitat-percolation-ecology-cluster-exponent`, `p-b-ising-social-dynamics-ewi`, `p-b-percolation-epidemiology-fss`, `p-b-percolation-oncology-gcc`
    - `docs/CROSSCHECK.md` contains column header text `CI grep CONFIRMED` and `RESULT contract`
    - `docs/CROSSCHECK.md` does not present `--all --write` as the happy-path command
    - `docs/CROSSCHECK.md` states there is no Crosscheck promote CLI (or “manual copy” + “no” + “promote CLI”)
    - File `docs/CROSSCHECK_PARITY.md` does not exist (D-11)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('docs/CROSSCHECK.md').read_text(encoding='utf-8'); assert 'python scripts/generate_crosscheck.py --bridge b-percolation-oncology --write' in t; assert 'drafts/crosscheck/physics-oncology/' in t; assert 'Run-mode parity' in t; assert 'Python is canonical; browser and Colab are demo tier' in t; ids=['p-b-habitat-percolation-ecology-fss','p-b-habitat-percolation-ecology-cluster-exponent','p-b-ising-social-dynamics-ewi','p-b-percolation-epidemiology-fss','p-b-percolation-oncology-gcc'];
[assert i in t, i for i in ids]; assert 'CI grep CONFIRMED' in t; assert 'RESULT contract' in t; assert 'python scripts/generate_crosscheck.py --all --write' not in t; assert not Path('docs/CROSSCHECK_PARITY.md').exists()"</automated>
  </verify>
  <done>CROSSCHECK.md documents the oncology generate command, manual promote (no CLI), and a D-05 parity table covering all four seeds plus GCC.</done>
</task>

<task type="auto">
  <name>Fix exit-code honesty, hub parity link, --apply/--check, changelog</name>
  <files>scripts/generate_repro_index_pages.py, repro/p-b-habitat-percolation-ecology-fss/README.md, dashboard/index.html, repro/p-b-percolation-oncology-gcc/index.html, dashboard/explainers/b-percolation-oncology.html, CHANGELOG.md, README.md, .planning/STATE.md</files>
  <read_first>scripts/generate_repro_index_pages.py, repro/p-b-habitat-percolation-ecology-fss/README.md, repro/p-b-ising-social-dynamics-ewi/README.md, dashboard/index.html, scripts/build_crosscheck.py, CHANGELOG.md, README.md, .planning/STATE.md</read_first>
  <action>
Implement D-06, D-07 (do not touch), D-15.

1. **D-15 generator.** In `scripts/generate_repro_index_pages.py`, replace the landing-page sentence that currently reads (template around the estimated-runtime paragraph):

```
  <p>Estimated runtime: {e(str(runtime))}. Exit code <code>0</code> = prediction within tolerance;
  <code>1</code> = falsified at stated precision.</p>
```

with a single paragraph (keep the runtime prefix):

```
  <p>Estimated runtime: {e(str(runtime))}. Exit code is always <code>0</code>; inspect stdout for CONFIRMED vs INCONCLUSIVE.</p>
```

Do not redesign the landing page. Do not change runner HTML. After this fix, `--apply` must stamp the honest sentence onto **all** repro `index.html` files, including the new oncology bundle.

2. **D-15 habitat FSS README.** In `repro/p-b-habitat-percolation-ecology-fss/README.md`, replace the line

`Exit code 0 = falsifiable prediction within tolerance; 1 = falsified.`

with the Ising wording:

`Exit code 0 always; inspect stdout for CONFIRMED vs INCONCLUSIVE.`

Do not change other seed READMEs unless they still claim exit 1 (cluster has no exit sentence — leave it). Do not copy the old habitat lie into the oncology README (03-01 already used Ising wording).

3. **D-06 hub static blurb.** In `dashboard/index.html`, inside `<section id="crosscheck">`, edit the **static** `<p class="section-desc">` that already links `../docs/CROSSCHECK.md`. Do **not** edit between `<!-- @hub-crosscheck-grid-begin -->` and `<!-- @hub-crosscheck-grid-end -->` (those cards come from `--apply`). Append a second link after the manifesto link:

```html
<a href="../docs/CROSSCHECK.md#run-mode-parity">Run-mode parity</a>
```

Do not add a new hub section, widget, or recommendation engine (Phase 5 / D-06). Do not redesign the dashboard.

4. **D-06 apply then check** (YAML from 03-01 already exists). From repo root, in order:

```
python scripts/build_crosscheck.py --apply
python scripts/build_crosscheck.py --check
python scripts/validate_schemas.py
```

Do not hand-edit `repro/p-b-percolation-oncology-gcc/index.html` or `dashboard/explainers/b-percolation-oncology.html`. Commit whatever `--apply` actually rewrites (new oncology landing, new oncology explainer, regenerated seed landings with the honest exit sentence, hub grid now including a fifth card labeled **Run repro**). If `--check` fails, re-run `--apply` and fix only sources (YAML / generator / static blurb), never the generated HTML by hand.

5. **D-07.** Do not modify `epidemic_percolation_fss.py` or freeze constants while regenerating artifacts.

6. **D-03 / D-16 / D-17 (still).** Do not add a 5th `grep -q "RESULT: CONFIRMED"` job. Do not add `tests/repo_smoke/test_generate_crosscheck.py`. Do not add `scripts/promote_crosscheck.py`.

7. **Docs deliverable.** `CHANGELOG.md` **Unreleased**: added oncology GCC protocol (status ready, INCONCLUSIVE stdlib demo), generate/promote path documented, run-mode parity matrix, exit-code honesty in index generator + habitat FSS README. `README.md` Crosscheck blurb currently says “3 seed protocols” — update to four CI-gated seeds plus the oncology GCC local demo (INCONCLUSIVE; not a fifth CONFIRMED). `.planning/STATE.md`: Phase 3 plan 02 complete; current focus Phase 4 next; note CROSS-06/07 closed pending verify-work.

8. If `docs/` changed: `mkdocs build --strict` (required).
  </action>
  <acceptance_criteria>
    - `scripts/generate_repro_index_pages.py` contains `Exit code is always` and does not contain `falsified at stated precision`
    - habitat FSS README contains `Exit code 0 always` and does not contain `1 = falsified`
    - `dashboard/index.html` static Crosscheck `section-desc` contains `run-mode-parity` (outside the hub-grid marker block is OK; the attribute/href must exist)
    - `repro/p-b-percolation-oncology-gcc/index.html` exists and contains `p-b-percolation-oncology-gcc` and `Exit code is always`
    - `dashboard/index.html` hub-grid block contains `p-b-percolation-oncology-gcc`
    - `dashboard/explainers/b-percolation-oncology.html` exists
    - `.github/workflows/crosscheck-repro.yml` still has exactly four `grep -q "RESULT: CONFIRMED"` and no `percolation-oncology`
    - `epidemic_percolation_fss.py` still has `NU_THEORY = 3.0`
  </acceptance_criteria>
  <verify>
    <automated>python scripts/build_crosscheck.py --check; python scripts/validate_schemas.py; mkdocs build --strict; python -c "from pathlib import Path; g=Path('scripts/generate_repro_index_pages.py').read_text(encoding='utf-8'); assert 'Exit code is always' in g; assert 'falsified at stated precision' not in g; r=Path('repro/p-b-habitat-percolation-ecology-fss/README.md').read_text(encoding='utf-8'); assert 'Exit code 0 always' in r; assert '1 = falsified' not in r; d=Path('dashboard/index.html').read_text(encoding='utf-8'); assert 'run-mode-parity' in d; assert 'p-b-percolation-oncology-gcc' in d; ix=Path('repro/p-b-percolation-oncology-gcc/index.html').read_text(encoding='utf-8'); assert 'p-b-percolation-oncology-gcc' in ix; assert 'Exit code is always' in ix; assert Path('dashboard/explainers/b-percolation-oncology.html').is_file(); w=Path('.github/workflows/crosscheck-repro.yml').read_text(encoding='utf-8'); assert w.count('grep -q \"RESULT: CONFIRMED\"')==4; assert 'percolation-oncology' not in w; assert 'NU_THEORY = 3.0' in Path('repro/p-b-percolation-epidemiology-fss/epidemic_percolation_fss.py').read_text(encoding='utf-8')"</automated>
  </verify>
  <done>Parity is linked from the hub; generated landings tell the exit-0 truth; --check is green; oncology card exists; epidemic freeze and four CI greps unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog YAML → generated HTML | `--apply` interpolates titles/ids into hub, explainers, repro landings |
| Docs manifesto → researcher claims | Parity table is the public statement of canonical vs demo tier |
| Static hub copy vs generated grid | Static `section-desc` is not in the `--check` hub-grid snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | `repro/*/index.html`, explainer HTML | mitigate | Only write via `build_crosscheck.py --apply`; `--check` snapshots |
| T-03-02-02 | XSS | hub/explainer titles | mitigate | Keep `html.escape` in generators; no hand-written card HTML |
| T-03-02-03 | Repudiation | exit-code docs | mitigate | D-15: generator + habitat README match `return 0` |
| T-03-02-04 | Tampering | epidemic freeze during regen | mitigate | D-07: do not edit `epidemic_percolation_fss.py` |
| T-03-02-05 | Elevation of privilege | promote/docs | mitigate | Document manual copy; no promote CLI (D-17) |
</threat_model>

<verification>
- `python scripts/build_crosscheck.py --check`
- `python scripts/validate_schemas.py`
- `mkdocs build --strict`
- Python asserts: four seed ids + GCC in `docs/CROSSCHECK.md`; generator honest exit sentence; four CI greps remain
</verification>

<success_criteria>
CROSS-07 parity matrix is in `docs/CROSSCHECK.md` with D-05 columns and all four seed ids; CROSS-06 generate command is documented; hub links `#run-mode-parity`; `--apply`/`--check` green; exit-code honesty holds on generated pages.
</success_criteria>

<output>
After completion, create `.planning/phases/03-crosscheck-scale-up/03-02-SUMMARY.md`
</output>
