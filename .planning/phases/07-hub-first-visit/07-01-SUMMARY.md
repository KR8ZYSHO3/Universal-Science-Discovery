# 07-01 Summary

Hub `#start` is the source of truth. USE.md, the three-door figure, README header, and dashboard README now use:

1. Run the test
2. See how two fields connect
3. Add an open question

Operator “keep it honest” is a footnote. Snapshot counts already matched git (`verify_dashboard_consistency`). Habitat FSS landing exists. Smoke: `tests/repo_smoke/test_first_visit_doors.py`.
