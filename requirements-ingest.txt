# Runtime dependencies for packages/ingest (arXiv OAI-PMH metadata only).
#
# Install CLI:
#   pip install -r requirements-ingest.txt && pip install -e ./packages/ingest
#
# Tests (fixture-based; no live HTTP by default):
#   pip install -e "./packages/ingest[dev]" && pytest
httpx>=0.27,<1
typer>=0.12,<1
