# Minimal root tool config (repo has no previous pyproject). Ingest package: packages/ingest/.
[tool.pytest.ini_options]
testpaths = ["packages/ingest/tests"]
pythonpath = ["packages/ingest/src"]
addopts = "-ra"
