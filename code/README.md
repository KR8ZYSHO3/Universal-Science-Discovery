# Code

Tooling: ingestion (arXiv / OpenAlex), validation, graph export, bots.

Add language-specific READMEs inside subfolders when you create them (`ingestion/`, `analysis/`, ...). Default quality targets for Python live in [cursorrules](../cursorrules).
