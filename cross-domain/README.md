# Cross-domain bridges

Interdisciplinary threads (e.g. `quantum-biology/`, `ai-for-science/`) linking disciplines under [disciplines/](../disciplines/).

Each subfolder should have a short README stating the bridge claim, key unknowns (`u-...`), and testable hypotheses (`h-...`).
